# Copy to .env and fill in. .env is git-ignored.
# The vault SERVES instructions; it does not call an LLM, so it needs no Anthropic key.

PORT=8000

# The Wall + Keyring + Control Panel (Phase 3/4)
# DATABASE_URL=            # Supabase Postgres connection string
# OAUTH_ISSUER=            # managed authorizer (Supabase Auth / Clerk / WorkOS)
# OAUTH_AUDIENCE=

# Phase 2 — ONLY if the vault proxies data calls so Qwintiq's keys never touch a consultant's
# machine (recommended for the AI-ARK skills). Otherwise these live in the consultant's Claude.
# AI_ARK_API_KEY=
# LEMLIST_API_KEY=
