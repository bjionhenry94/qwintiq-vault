# Pinned to the mature 1.x FastMCP line (2.0.0 restructured the high-level API).
mcp>=1.9.0,<2.0.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.30
httpx>=0.27
# Starlette is a DIRECT dependency (server.py/panel.py/oauth.py import it) — pin explicitly
# rather than relying on it arriving transitively via mcp.
starlette>=0.37,<2
# Server-side execution (finished-output-only): the vault runs the skills through a model.
# Use EITHER an Anthropic key OR an OpenAI (ChatGPT) key — set in /admin or as an env var.
anthropic>=0.40
openai>=1.40
# Encrypts admin-set API keys at rest in the DB (Fernet, keyed off SECRET_KEY).
cryptography>=42
# Prod DB (Supabase Postgres). Dev falls back to SQLite with no extra install.
psycopg[binary]>=3.1
